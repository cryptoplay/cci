#!/bin/bash
set -e

BUILD_DATE=$(date +%Y%m%dT%H%M)
VCS_REF=${CIRCLE_SHA1:0:7}
TAG=${BUILD_DATE}-git-${VCS_REF}

docker login -u ${user} -p ${pass}
docker tag cryptoplay/cci cryptoplay/cci:${TAG}
docker push cryptoplay/cci:${TAG}
docker push cryptoplay/cci:latest
docker logout
